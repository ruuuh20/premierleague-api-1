import React from 'react';

const FixtureInfo = (props) => {
  if (props.id) {
    return (

    <div>
      <h1>hi</h1>

    </div>
  )
  }
  else {
    return null
  }

}

export default FixtureInfo;
