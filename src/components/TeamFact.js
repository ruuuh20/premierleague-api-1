import React from 'react';

const TeamFact = props => {
  let page = null;

  switch (props.type) {
    case ('first-page'):
    page = (
      <div className="first-page">
        </div>
    )
  }

  return (


  )
}

export default TeamFact;
